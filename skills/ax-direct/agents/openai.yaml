interface:
  display_name: "AX Direct"
  short_description: "Write direct, natural replies that are easy to scan"
  default_prompt: "Use $ax-direct for clear, direct replies throughout this session."
policy:
  allow_implicit_invocation: false
